# powerjudge-server
Online judge server

Requirements.

Install `jq`

```bash
brew install jq
```


Setup Docker on the MacOS

1. Docker Preferences
2. File sharing
3. Add `provisioning` directory from `conf` directory.
